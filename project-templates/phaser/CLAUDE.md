# {{PROJECT_NAME}}

Phaser 4 game project. Replace `{{PROJECT_NAME}}` and fill in the sections below.

## Architecture

(Scenes, entry point, asset pipeline — brief overview.)

## Commands

- Install: `npm install` (or `pnpm install`)
- Dev: `npm run dev`
- Build: `npm run build`
- Test: `npm test`

## Conventions

- Phaser version: 4.x — see the `v3-to-v4-migration` skill if porting v3 code.
- 28 Phaser skills are pre-installed under `.claude/skills/` and auto-trigger on relevant API mentions (scenes, physics, tilemaps, tweens, particles, etc.). No manual invocation needed.
- Package manager: npm or pnpm (specify in lockfile).
